# Go Mode Android App

Go Mode is a Kotlin/Compose Android app with package `com.fghbuild.gomode`.
It is a thin native shell around backend-hosted mobile web frontends.

Read `../AGENTS.md` first for shared Android rules. Use
`../docs/WEB_SHELL.md` as the canonical architecture plan.

## Product Boundary

Go Mode is the Android app for backend-hosted mobile frontends.

Go Mode owns native platform capabilities:
- Bootstrap settings before a WebView can load.
- WebView hosting and host-mode compatibility negotiation.
- Android notification permission and hosted-service notifications.
- Microphone/audio routing and foreground voice services.
- Screenshot capture when native MediaProjection is required.
- Halo/BLE integration when the shell owns the native capability.

The backend-hosted web frontend owns screen-mode product UI. Do not copy caic
task list, task detail, diff, process, widget, or screen-mode Compose UI into
Go Mode.

Go Mode must not depend on caic domain APIs or SDK types. In Go Mode app code
and tests, do not import `com.caic.sdk.v1`, create/list/input caic tasks, or
assert caic-specific routes such as `/task/...`. Treat caic as one possible
hosted web app, opaque behind the WebView.

## Architecture

```text
MainActivity
  -> GoModeApp
      -> native settings fallback
      -> active service instance
      -> WebShellScreen
          -> WebView loading backend-hosted frontend
```

The target architecture also includes app-scoped monitoring, notifications,
voice endpoint behavior, screenshot capture, and Halo/BLE. Add these as native
capabilities only when the WebView shell needs them.

## Current Shape

- `MainActivity.kt`: single-activity host.
- `data/SettingsRepository.kt`: persisted service-instance settings.
- `ui/GoModeApp.kt`: root Compose surface.
- `ui/settings/SettingsScreen.kt`: native settings fallback.
- `ui/web/WebShellScreen.kt`: WebView shell.

## Rules

- Package/application ID is `com.fghbuild.gomode`; do not reuse caic package
  names, DataStore names, notification channels, deep-link schemes, or test
  package names.
- Keep service identity explicit. Settings should be keyed by service type and
  service instance, not only by URL.
- Load backend-hosted web frontends; do not bundle frontend assets into the APK.
- Do not link Go Mode against caic API clients, generated SDKs, task DTOs, task
  routes, or other caic product concepts.
- Do not proxy normal caic API calls through a JavaScript bridge.
- Keep the JavaScript bridge narrow, versioned, and capability-oriented.
- Android back handling must account for SPA route ownership, not only
  `WebView.canGoBack()`.
- Platform WebViews need real Android resource IDs for instrumented tests.

## Instrumented Testing

`make android-check` compiles the app and instrumented tests. `make android-e2e`
starts or reuses an emulator, serves a generic hosted frontend fixture, and
runs shell-owned instrumented behavior. New instrumented coverage should
validate shell-owned behavior through a generic hosted frontend. Mark tests
that use the standalone fixture with `@StandaloneHostedFixture`; the E2E runner
selects that annotation and excludes host-specific cases.

## Checks

| Change | Required validation |
| --- | --- |
| Any Go Mode Android change | `make android-check` |
| Networking, SSE, service monitoring, WebView loading, settings, host-mode behavior, or native shell routing | Focused Robolectric tests and `make android-check` |

<!-- BEGIN FILE INDEX -->
## File Index

Autogenerated from first-line comments. Run scripts/update_agents_file_index.py to refresh.

- `src/androidTest/java/com/fghbuild/gomode/GoModeDocumentationScreenshotsTest.kt`: Generates documentation screenshots for Go Mode native shell and hosted caic task surfaces.
- `src/androidTest/java/com/fghbuild/gomode/GoModeE2eTestBase.kt`: Shared instrumented helpers for Go Mode hosted WebView e2e coverage.
- `src/androidTest/java/com/fghbuild/gomode/StandaloneHostedFixture.kt`: Marks Android tests that run against the standalone generic hosted frontend fixture.
- `src/androidTest/java/com/fghbuild/gomode/WebShellSmokeTest.kt`: Instrumented smoke coverage for the Go Mode WebView shell and native service monitoring.
- `src/androidTest/java/com/fghbuild/gomode/voice/VoicePanelTest.kt`: Compose UI tests for the compact and expandable Go Mode voice panel transcript.
- `src/main/java/com/fghbuild/gomode/MainActivity.kt`: Single activity host for the Go Mode WebView shell.
- `src/main/java/com/fghbuild/gomode/data/SettingsRepository.kt`: Persisted Go Mode service-instance settings backed by DataStore preferences.
- `src/main/java/com/fghbuild/gomode/halo/HaloController.kt`: HaloController manages Go Mode Halo BLE scanning, connection state, and selected-device settings.
- `src/main/java/com/fghbuild/gomode/service/CredentialedHTTPClient.kt`: Provides an HTTP client that keeps manually supplied service credentials on the original request.
- `src/main/java/com/fghbuild/gomode/service/ServiceMonitor.kt`: ServiceMonitor turns MCP resources into native attention, service notification, and voice context state.
- `src/main/java/com/fghbuild/gomode/service/ServiceNotificationPublisher.kt`: ServiceNotificationPublisher posts monitored service events through Go Mode's native alert channel.
- `src/main/java/com/fghbuild/gomode/service/ServiceResources.kt`: Service resource parsing turns generic Go Mode MCP resources into native monitoring state.
- `src/main/java/com/fghbuild/gomode/service/ServiceSettings.kt`: Service discovery client, URL helpers, and compatibility checks for the Go Mode root settings document.
- `src/main/java/com/fghbuild/gomode/service/VoiceTokenClient.kt`: Fetches a host-issued scoped authorization block for standalone voice gateways.
- `src/main/java/com/fghbuild/gomode/ui/GoModeApp.kt`: Root Compose surface that bootstraps service settings and coordinates shell recovery with voice controls.
- `src/main/java/com/fghbuild/gomode/ui/halo/HaloScreen.kt`: Compose Halo screen for Go Mode native BLE device management.
- `src/main/java/com/fghbuild/gomode/ui/settings/SettingsScreen.kt`: Native settings fallback for configuring the active Go Mode service instance.
- `src/main/java/com/fghbuild/gomode/ui/theme/Theme.kt`: Material theme for the Go Mode Android shell.
- `src/main/java/com/fghbuild/gomode/ui/web/ServiceBearerStore.kt`: Holds hosted-service auth signals in memory for native requests to the active service origin.
- `src/main/java/com/fghbuild/gomode/ui/web/WebShellScreen.kt`: WebView shell that reports host state and accepts origin-scoped bearer tokens from the hosted frontend.
- `src/main/java/com/fghbuild/gomode/voice/McpClient.kt`: MCP client for Go Mode service tools and resources exposed to the native shell.
- `src/main/java/com/fghbuild/gomode/voice/VoiceOverlay.kt`: Full-width bottom shell panel composable: settings, voice controls, attention, and transcript display.
- `src/main/java/com/fghbuild/gomode/voice/VoiceService.kt`: Foreground service that keeps voice active and surfaces service attention while using the microphone.
- `src/main/java/com/fghbuild/gomode/voice/VoiceSession.kt`: Manages Go Mode voice gateway sessions via WebRTC, service MCP tools, and Android audio routing.
- `src/test/java/com/fghbuild/gomode/data/SettingsRepositoryTest.kt`: Unit tests for Go Mode service-instance settings.
- `src/test/java/com/fghbuild/gomode/service/ServiceMonitorTest.kt`: Unit tests for Go Mode service monitoring resource orchestration.
- `src/test/java/com/fghbuild/gomode/service/ServiceResourcesTest.kt`: Unit tests for generic Go Mode service monitoring resources.
- `src/test/java/com/fghbuild/gomode/service/ServiceSettingsClientTest.kt`: Unit tests for Go Mode service settings fetching and compatibility validation.
- `src/test/java/com/fghbuild/gomode/service/VoiceTokenClientTest.kt`: Tests host-issued scoped voice token retrieval for standalone gateway sessions.
- `src/test/java/com/fghbuild/gomode/ui/GoModeAppTest.kt`: Unit tests for Go Mode shell recovery and voice account changes.
- `src/test/java/com/fghbuild/gomode/ui/web/ServiceBearerStoreTest.kt`: Tests active-service and WebView-origin checks for in-memory auth messages.
- `src/test/java/com/fghbuild/gomode/ui/web/WebShellScreenTest.kt`: Unit tests for Go Mode WebView recovery and new-window routing helpers.
- `src/test/java/com/fghbuild/gomode/voice/McpClientTest.kt`: Unit tests for Go Mode MCP client requests and SSE configuration.
- `src/test/java/com/fghbuild/gomode/voice/VoiceSessionLifecycleTest.kt`: Tests that disconnected voice attempts cannot continue discovery or deliver stale tool results.
- `src/test/java/com/fghbuild/gomode/voice/VoiceSessionTest.kt`: Unit tests for Go Mode voice session diagnostics and service URL resolution.
<!-- END FILE INDEX -->
