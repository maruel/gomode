# Halo SDK

Kotlin/Android SDK for Brilliant Halo (and Frame) smart glasses over Bluetooth LE.
Mirrors the [Brilliant SDK](https://github.com/brilliantlabsAR/brilliant_sdk) in native
Kotlin using `android.bluetooth.le`. BLE protocol constants and wire-format details live in
`HaloProtocol`, `HaloDevice`, and the typed message classes, so the SDK code is the reference.

## Architecture

```
Host App
  └── com.caic.halo.msg  —  typed messages (sprites, text, camera, IMU, photos, clicks, taps, audio)
      └── com.caic.halo.ble  —  BLE transport (connect, MTU-aware chunking, Lua REPL)
          └── Android Bluetooth LE
              └── Halo device (Lua VM + frame.* API)
```

## Module

Gradle module `:halo-sdk` at `sdk/halo/`:

```kotlin
dependencies { implementation(project(":halo-sdk")) }
```

## Design Decisions

**Channel + `receiveAsFlow()` for test notifications.** `MutableSharedFlow(replay=0)` drops
values emitted before a collector starts. `Channel.UNLIMITED` buffers values and delivers
to cold flow collectors — matches send-then-receive pattern.

**`open` class with protected overridable methods.** `BluetoothGatt` has no settable
callback. Tests subclass `HaloConnection` and override `connectGatt()`, `createHaloDevice()`,
`wireWriteSink()`, and `onMtuNegotiated()` to inject test doubles.

## Test Strategy

| Layer | Approach |
|-------|----------|
| Pure logic (`HaloServiceDiscovery`, bit packing, Rx parsers) | JVM unit tests with Robolectric |
| Flow-based logic (`HaloDevice` send/receive) | JVM unit tests, `Channel`-backed notification flow |
| GATT integration (`HaloConnection` lifecycle) | Subclass-override tests |
| Actual BLE I/O | Deferred until real device (instrumented tests) |

## Firmware and codec scope

- Audio transport supports PCM or pre-encoded LC3 frames. Native LC3 encode/decode is left to apps
  that bundle `liblc3`.
- OTA/SMP UUIDs are exposed for discovery. Firmware transfer is intentionally not part of this app SDK.

<!-- BEGIN FILE INDEX -->
## File Index

Autogenerated from first-line comments. Run scripts/update_agents_file_index.py to refresh.

- `src/main/kotlin/com/caic/halo/ble/HaloConnection.kt`: HaloConnection: BLE scanning, connection, bonding, MTU negotiation, service discovery, and GATT writes for Halo/Frame devices.
- `src/main/kotlin/com/caic/halo/ble/HaloDevice.kt`: HaloDevice: connected Halo/Frame over BLE — Lua REPL, raw data, control signals, audio streaming, typed messages, file upload.
- `src/main/kotlin/com/caic/halo/ble/HaloDeviceType.kt`: Halo Bluetooth LE device types: Frame, Halo, or unknown (pre-connection).
- `src/main/kotlin/com/caic/halo/ble/HaloEmulatorBridgeClient.kt`: HaloEmulatorBridgeClient: WebSocket client for the Python Halo emulator bridge.
- `src/main/kotlin/com/caic/halo/ble/HaloException.kt`: Exception thrown by the Halo BLE library for all transport-level errors.
- `src/main/kotlin/com/caic/halo/ble/HaloProtocol.kt`: HaloProtocol: BLE UUIDs, control bytes, payload limits, and typed-message framing for Brilliant Halo/Frame devices.
- `src/main/kotlin/com/caic/halo/ble/HaloScannedDevice.kt`: Scan result produced by HaloScanner, carrying the platform BluetoothDevice and RSSI.
- `src/main/kotlin/com/caic/halo/ble/HaloServiceDiscovery.kt`: Pure functions for parsing GATT services/characteristics from a Halo or Frame device.
- `src/main/kotlin/com/caic/halo/msg/HaloMessageCodes.kt`: HaloMessageCodes: default Brilliant message-code assignments for host↔device typed messages.
- `src/main/kotlin/com/caic/halo/msg/HalosideApp.kt`: HalosideApp: manages the full haloside application lifecycle on the device.
- `src/main/kotlin/com/caic/halo/msg/RxAudio.kt`: RxAudio: reassemble or stream audio chunks from dataResponse and emit WAV bytes for PCM clips.
- `src/main/kotlin/com/caic/halo/msg/RxAutoExpResult.kt`: RxAutoExpResult: parse auto-exposure and white-balance results from dataResponse.
- `src/main/kotlin/com/caic/halo/msg/RxClick.kt`: RxClick: parse Halo button click events from dataResponse.
- `src/main/kotlin/com/caic/halo/msg/RxIMU.kt`: RxIMU: parse IMU data (6 × float32) from dataResponse.
- `src/main/kotlin/com/caic/halo/msg/RxMeteringData.kt`: RxMeteringData: parse camera metering samples from dataResponse.
- `src/main/kotlin/com/caic/halo/msg/RxPhoto.kt`: RxPhoto: reassemble JPEG photo chunks from dataResponse. Emits a single-element Flow.
- `src/main/kotlin/com/caic/halo/msg/RxTap.kt`: RxTap: group Frame tap packets into multi-tap counts from dataResponse.
- `src/main/kotlin/com/caic/halo/msg/TxAutoExpSettings.kt`: TxAutoExpSettings: automatic exposure and white-balance settings sent to the device.
- `src/main/kotlin/com/caic/halo/msg/TxCaptureSettings.kt`: TxCaptureSettings: camera capture request settings sent to the device.
- `src/main/kotlin/com/caic/halo/msg/TxCode.kt`: TxCode: send a single byte value to the device.
- `src/main/kotlin/com/caic/halo/msg/TxManualExpSettings.kt`: TxManualExpSettings: manual camera exposure and gain settings sent to the device.
- `src/main/kotlin/com/caic/halo/msg/TxMessage.kt`: TxMessage: base type for messages the host sends to the device via HaloDevice.sendMessage().
- `src/main/kotlin/com/caic/halo/msg/TxPlainText.kt`: TxPlainText: send plain text for display on the device.
- `src/main/kotlin/com/caic/halo/msg/TxSprite.kt`: TxSprite: indexed-color sprite sent to the device — PNG decoding, palette quantization, 1/2/4bpp pixel packing.
- `src/main/kotlin/com/caic/halo/msg/TxTextPage.kt`: TxTextPage: layout and rasterize text pages for rectangular Frame and circular Halo displays.
- `src/test/kotlin/com/caic/halo/ble/HaloConnectionTest.kt`: Unit tests for HaloConnection device type detection and service wiring logic.
- `src/test/kotlin/com/caic/halo/ble/HaloDataTypesTest.kt`: Unit tests for HaloDeviceType and HaloException — pure Kotlin, no Android dependencies.
- `src/test/kotlin/com/caic/halo/ble/HaloDeviceTest.kt`: Unit tests for HaloDevice: Lua REPL, raw data, control signals, typed messages, file upload.
- `src/test/kotlin/com/caic/halo/ble/HaloEmulatorBridgeClientTest.kt`: Unit tests for HaloEmulatorBridgeClient WebSocket request/response and event handling.
- `src/test/kotlin/com/caic/halo/ble/HaloServiceDiscoveryTest.kt`: Unit tests for HaloServiceDiscovery — pure functions, no Android BLE I/O.
- `src/test/kotlin/com/caic/halo/msg/HalosideAppTest.kt`: Unit tests for HalosideApp bundled Lua assets.
- `src/test/kotlin/com/caic/halo/msg/RxMessageTest.kt`: Unit tests for Rx message parsers.
- `src/test/kotlin/com/caic/halo/msg/TxMessageTest.kt`: Unit tests for TxMessage types: wire format packing.
<!-- END FILE INDEX -->
